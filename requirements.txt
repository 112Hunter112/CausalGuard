# Core
python-dotenv==1.0.0
numpy==1.26.4
scipy>=1.11,<1.14
pydantic==2.7.0

# Sentence transformers (Layer 3 — runs locally, no API)
# Use CPU-only torch for smaller image (Railway/deploy). For GPU: pip install torch
sentence-transformers==3.0.1
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.0+cpu
transformers==4.41.0

# Neural ODE (Layer 5 — Chen et al. NeurIPS 2018). Optional; fallback Euler if missing.
torchdiffeq>=0.2.0

# LLM API (Vertex AI via gcloud CLI credentials)
anthropic[vertex]>=0.84.0
google-genai>=1.0.0

# Gmail API (live email demo)
google-api-python-client>=2.100.0
google-auth>=2.25.0

# Terminal UI
rich==13.7.1

# Datasets (for download_datasets.py and training on InjecAgent / deepset / LLMail)
datasets>=2.14.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7

# Web dashboard (stretch goal)
flask==3.0.3
flask-cors==4.0.1
flask-socketio==5.3.6
