[package]
name = "causalguard_scanner"
version = "0.1.0"
edition = "2021"

[lib]
name = "causalguard_scanner"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
regex = "1"
